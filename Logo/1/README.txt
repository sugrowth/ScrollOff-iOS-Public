
# ScrollOff Logo Pack — v4 (Minimal / SO Monogram)

Design goals:
- Clean, brandable monogram (S carved inside a power-O).
- No awkward spacing — wordmark is a single text node with tight letter-spacing.
- High-contrast palette (charcoal + lime) that also works in mono.

Files
1. `scrolloff-primary-horizontal.svg` — Horizontal lockup
2. `scrolloff-banner.svg` — Wide banner
3. `scrolloff-square-icon.svg` — App icon
4. `scrolloff-logomark.svg` — Mark only
5. `scrolloff-favicon.svg` — Simplified small-size icon

Colors (edit inside each SVG):
- `--bg`   #0B0F14
- `--ink`  #F8FAFC
- `--lime` #A3E635
- `--violet` #7C3AED (secondary, optional)

If you want alternatives (blue/coral, orange/ink, or grayscale), I can export variants right away.
